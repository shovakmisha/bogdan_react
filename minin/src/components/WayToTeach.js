function WayToTeach({ title, description }) {
    return (

        <li className="way-to-tech-li">
            <p><strong>{title}</strong> {description}</p>
        </li>

    )
}

export default WayToTeach;